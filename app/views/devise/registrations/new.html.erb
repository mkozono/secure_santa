<h2>Sign up</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  No email or password required.  Just sign in with your Facebook account!

<% end %>

<%= render "devise/shared/links" %>
